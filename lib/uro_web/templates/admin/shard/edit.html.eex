<h1><%= gettext("Edit Shard") %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_shard_path(@conn, :update, @shard)) %>

<span><%= link gettext("Back"), to: Routes.admin_shard_path(@conn, :index) %></span>

# This file is used for advanced configuration when environment variables aren't enough.
# To use it, copy this file to `config/local.exs` and edit it to your liking, it is ignored by git.
import Config
